
 priority_queue<pair<long long,int>, vector<pair<long long,int>>,greater<pair<long long,int>>> pq;
 vector<bool>visited(n,0);
  
 long long ans=0;
 pq.push({0, 0});
 
 while(!pq.empty()) {
    auto p = pq.top();
    pq.pop();

    long long wt = p.first;
    int u = p.second;

    if(visited[u]) continue;
     
    ans+=wt;
    visited[u]=1;

    for(auto &x : adj[u]) if(!visited[x.first]) pq.push({x.second,x.first});
 }

 cout<<ans;